# Mirror tool configuration

# Local Windows source directory to mirror
source: "C:\\Projects\\myapp"

# Remote Linux host (user@hostname or just hostname)
host: "user@linuxbox"

# Remote destination directory
dest: "/home/user/mirror"

# Default poll interval in seconds (used when push/pull intervals are not set)
interval: 5

# Per-direction poll intervals (optional, override interval)
# push_interval: 5
# pull_interval: 30

# SSH port
ssh_port: 22

# Modification time tolerance in seconds (files with mtime differences
# within this threshold are considered unchanged)
mtime_tolerance: 2

# File/directory patterns to exclude from sync (fnmatch syntax)
# Patterns are matched against both filenames and relative paths
excludes:
  - ".git"
  - "__pycache__"
  - "*.pyc"
  - "node_modules"
  - ".env"

# Pull a remote directory to local (optional, local_dest can be a symlink to a dir)
# pull_source: "/home/user/configs"
# pull_dest: "C:\\Users\\me\\configs"
